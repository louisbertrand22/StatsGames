name: "Feature: Add Authentication"
description: "Implement user authentication using Supabase Auth for the StatsGames MVP."
title: "[Auth] Implémenter l’authentification"
labels: ["feature", "auth", "backend"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        ## 🎯 Objectif  
        Implémenter l’authentification complète pour l’application StatsGames en utilisant **Supabase Auth**.

  - type: textarea
    id: goal
    attributes:
      label: "🎯 Résultat attendu"
      description: "Décris ce que l’auth doit permettre."
      placeholder: |
        Exemple :
        - Sign in / Sign up
        - Magic link
        - Auth UI côté front
        - Récupération des données du profil utilisateur
    validations:
      required: true

  - type: textarea
    id: tasks
    attributes:
      label: "🛠️ Tâches à réaliser"
      description: "Liste les étapes nécessaires."
      placeholder: |
        Exemple :
        - Activer Email Auth dans Supabase
        - Créer un endpoint /auth/callback
        - Ajouter l’AuthContext côté front
        - Ajouter un composant LoginView
        - Protéger les routes privées
    validations:
      required: true

  - type: dropdown
    id: priority
    attributes:
      label: "📌 Priorité"
      options:
        - High
        - Medium
        - Low
      default: 0

  - type: textarea
    id: notes
    attributes:
      label: "📝 Notes supplémentaires"
      placeholder: "Ajoute tout détail utile ici."
